import { IUser } from '@/types'
import SearchBarHeader from '../common/SearchBarHeader'
import UserMenu from '../common/UserMenu'

const TopSidebar = ({ userData }: { userData: IUser }) => {
  return (
    <div className="fixed left-[300px] right-0 top-0 z-20">
      <div className="flex h-16 items-center justify-between gap-5 px-5 py-3 shadow-sm">
        <div>
          <p className="font-medium">Xin chào, {userData?.name}</p>
        </div>

        <div className="flex items-center space-x-4">
          <SearchBarHeader />
          <UserMenu userData={userData} />
        </div>
      </div>
    </div>
  )
}

export default TopSidebar
